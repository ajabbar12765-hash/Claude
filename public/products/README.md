# Product photos

Place your product images in this folder (e.g. `room-spray.jpg`), then set the
matching `image` field in `src/App.jsx`'s `PRODUCTS` list, for example:

```js
image: '/products/room-spray.jpg',
```

Until an image is set, the site shows an elegant "photo coming soon" placeholder.
