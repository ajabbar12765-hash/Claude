# Spec/demo deployment — this pitch build is kept out of search indexes so it
# cannot be mistaken for RALLY's official site. Swap Disallow for Allow (and
# remove the noindex meta in index.html) when the client takes it live.
User-agent: *
Disallow: /

Sitemap: https://rally.example.com/sitemap.xml
